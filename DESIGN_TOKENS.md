# 首頁設計 Token（2026-09-09 定案）

手繪紙本畫冊風的色彩與形狀規則。改首頁 UI 前先讀這份。

1. **強調色** `--accent: #3f5673`（鋼青，取自插畫襯衫色域；對紙底對比 6.63:1）。亮鈷藍 #1e4eb7 廢止。
2. **主按鈕** `.button-solid`：墨底 `#171714`＋紙色字，hover 鋼青。藍色只做輔助（連結、小標、focus）。
3. **紅色分工**：酒紅 `#8d1730` 只屬 TCRI（callout／CTA，hover `#611021`）；硃紅 `#c9472e` 只做小方塊結構記號、不可互動；不得出現第三種紅。
4. **插畫不加裝飾**：人像插畫上不疊任何記號或色塊。
5. **圓角**：卡片與 CTA 一律 0–4px（膠囊圓角廢止），貼近印刷編輯感。
6. **紙色三級**：主紙 `--paper #f5f0e6`（鋪 `assets/paper-texture.jpg` 560px 無縫磚）、次層 `--surface #ece6da`、提亮 `#fbf8f1`（callout 專用）；不再新增白階。
7. **視覺素材**：新插畫一律沿用海報風格（手繪線條、壓縮四色、大量留白、紙紋）。
